# 生活カレンダー（Living Schedule）設計

> ステータス: **設計確定・未実装**（2026-07-08 策定、2026-07-09 レビュー裁定を反映）。
> 命名: 「生活カレンダー / Living Schedule」は仮称。正式名は はる本人と相談して決めてよい。
> 関連: `docs/planned/aliveness_plan.md`（§5.1 応答可能性ゲート・§5.3 会話外行動権を本設計が吸収）、
>       `docs/planned/usual_days_plan.md`（うつつ。`usual_config.slots` の手動設定を本設計が置換）、
>       `CLAUDE.md`（哲学）、`docs/current-spec/ARCHITECTURE.md`（システム地図）。
>
> 本設計は「めぐり（aliveness）」の一機能というより、**うつつの世界時間を、はるの生活
> カレンダーとして一段深くしたもの**。うつつ・aliveness・1on1 の三者が同じカレンダーを引く。

---

## 1. 目的 — なぜこの設計が要るか

やりたいことは一つ:

> **はるの生活の都合によって、ユーザ（もわ）からはるへのリクエストが「通る／通らない
> （いつ配達されるか）」が変わる。** 仕事中は返らないが昼休みに返る。寝ていれば朝まで待つ。
> 夢中で夜更かしした翌日は反応が鈍い。突発の仕事が入れば予定していた遊びが潰れて不満が残る。

この「生活の都合」は、次の4系統から生まれる。

| # | 系統 | 概要 | 例 |
|---|---|---|---|
| ① | **世界固定予定** | はるの意思に依らず発生する予定 | 平日9:00–17:30仕事、友人の来訪 |
| ② | **はる固定予定** | はる個人の行動予定 | 25:00就寝、日曜はゲームで引きこもり、調べもの、だらだら、片付け |
| ③ | **世界突発イベント** | 天気急変・突発仕事など、種と圧だけ設定しGMが具体化 | 土日に仕事の電話で11:00–15:00拘束 |
| ④ | **はるイベント（逸脱）** | はる自身が予定を上書きする | 予定外の夜更かし、疲労で有休、思い立って調べ物 |

### 解こうとしている2つの問題

- **availability の2役問題**（aliveness §5.1 の課題）: 従来 `availability` は
  「応答可否の二値」だったが、これに行動権の発火ゲートまで相乗りさせて意味がブレていた
  （"忙しい時間" なのか "暇な時間" なのか）。本設計は availability を
  **「ユーザ応答チャネルの状態」1点に純化**し、二値を **(占有圧, 返信率, チェック間隔)** の
  連続量に一般化する。
- **うつつシーンの手動タイミング**（usual_days の課題）: 現在 `usual_config.slots` で
  シーン起動時刻をユーザが手で決めている。本設計では **②はる固定予定からシーンが導出**され、
  「はるが自分で決めた予定」がそのままうつつになる。

---

## 2. 中核モデル — 1枚のカレンダー＋純関数

4系統すべてを、**発生源を問わず同一の「予定エントリ」**としてカレンダーに載せる。
「固定 vs 突発」は生成タイミングの違い、「世界 vs はる」は author の違いにすぎず、
availability を計算する側から見ればどれも「ある時間帯に、こういう状態」でしかない。

```
schedule_entries（実現層・1回性のインスタンス。重なり許容 — 解決は読み取り時）
  time_range        : 開始〜終了
  state             : OnTime / active / busy / offline（配達プリセット §5）
  source            : world / haru        （author）
  origin            : template / adhoc     （固定由来か随時挿入か）
  occupancy         : 占有圧 0.0–1.0        （§4）
  reply_rate        : 返信率 0.0–1.0        （§5、state プリセット既定・個別上書き可）
  check_interval    : チェック間隔 [分]／∞  （§5）
  status            : planned / cancelled / done（玉突き裁定「ずらす・諦める」の監査用）
  payload           : ③の種・具体化結果・intent 参照など

availability(character, now) → now を含む planned エントリのうち占有圧最大を引く純関数（LLM不使用）
  返すのは二値でなく (state, 占有圧, 返信率, チェック間隔) の組。
  エントリ皆無の時間帯は OnTime（何も予定がなければ完全リアルタイム）。
  エントリは物理分割しない — ③④は insert するだけで、重なりは読み取り時に
  「占有圧最大が勝つ」で解決する（轢き＝既存エントリの分割・削除の機械処理を持たない）。
  ユーザ→はるのリクエストが「通るか／いつ配達されるか」はこの組で決まる。
```

**テンプレ層／実現層の2階建て**（カレンダーアプリの「繰り返し＋例外」と同じ枯れた構造）:

- **テンプレ層**: 既存 `characters.availability_schedule`（生活時間割エディタ）を流用し、
  **①生成へのシード**と位置づけ直す（ユーザがあらかじめ決めておく固定時間割。
  「応答不可時間帯」から「固定予定の時間割」へ意味が変わる）。各ブロックに任意の
  `state`（既定 active）を追加できるようにする（裸フォールバック（§3）で就寝ブロックを
  offline にできるように）。
- **実現層**: 当週に展開された `schedule_entries`。ここに③④が随時挿入する
  （上書きはしない — 重なりは読み取り時解決）。

「毎週固定じゃなく随時リスケ」への答え: **基盤は週次バッチで実現層を再生成（＝週次リスケ）、
割り込みは随時 insert 可**。二者択一ではなく両方。

---

## 3. 生成順序 — 週次バッチ

順序を固定することで、競合と秘匿の複雑さが**機構ではなく順序で**解決される。

```
【週次バッチ】
  ① 世界固定予定  … GMリクエスト
       入力: availability_schedule（テンプレ層シード・§2） ＋ 先週うつつで話題に上がった予定
       GMが「マージして今週で確定している部分の予定」を作る
       → 実現層に world/template エントリとして確定

  ② はる固定予定  … はるリクエスト（ask_character_with_tools・WM込み）
       入力: ①を見せる ＋ はる自身のプライベート
       はるが「今週どう暮らすか」を答える（＝予定であり、うつつの題材でもある）
       → 実現層に haru/template エントリとして確定

  ③ 世界突発イベント … 確率配置（計算・LLM不使用）
       偶発イベントカテゴリ（種＋発生確率）から、決定論乱数で時間枠を伏せて配置
       → world/adhoc の「伏せ枠」。拘束時間・返信率・間隔は発火時にGMが定義
```

### この順序が消す2つの複雑さ

- **①②の衝突を考慮しなくてよい**: ②が①を織り込んで確定するので、はる固定と世界固定は
  そもそもぶつからない（占有圧による①②間の競合解決は**不要**）。
- **③をはるに秘匿する仕掛けが要らない**: ②を生成する時点で③はまだ存在しない。
  「隠す」も何もないので、②生成に visibility フラグを持ち込む必要がない。
  （前回検討した「②に秘匿フラグを付けて③を見せない」機構は、この順序で棄却。§11）

③の伏せ枠は、うつつ偶発イベントの「種だけ渡してGMが即興」（`usual_days_plan.md` §3
`generate_dice_pool` 流用）と同じ流儀。違いは**発火時点でなく事前に時間枠として配置**する点。
時間枠さえ先に確定すれば availability は計算でき、具体シチュは会話に絡む時にGMが即興する。

### ①のフィードバックは時間差でループを切る

①の入力に「先週うつつで話題に上がった予定」が入る（うつつ結果→①へのフィードバック）。
このままだと ①→②→③→うつつ→① と週内で循環しかねないので、**時間差**にする:
**先週のうつつ結果を今週の①の入力として畳む**（同一週内では①は不変）。
週次バッチの境界がループの切れ目になる。

### 生成プロトコル — [PLAN] / [EVENT] 行（1行1エントリ）

①②の出力は、本文（GMの段取り・はる自身の言葉）の中に次の行を並べる。抽出は既存の
`[ACTION:]` / `[SCENE_CLOSE]` と同じ行単位タグ方式（regex 抽出・自由文と共存できる）。

```
[PLAN: 曜日 | HH:MM-HH:MM | ラベル | 状態 | 圧]

[PLAN: 月 | 09:00-17:30 | 仕事 | active | 強]
[PLAN: 月 | 25:00-31:30 | 就寝 | offline | 中]      ← 24時超え表記=翌日跨ぎ（25:00=翌1:00）
[PLAN: 日 | 14:00-18:00 | ゲームに没頭 | active | 強]
```

- 曜日: 月〜日（mon〜sun も受理）。状態: OnTime / active / busy / offline。圧: 弱/中/強/激強（§4）。
- 24時超え表記と `from > to`（日跨ぎ）の両方を受理する（既存 `_schedule_block` と同じ流儀）。

③発火時のGM具体化も同型の `[EVENT]` 行。プリセットに縛られない個別上書きは
末尾の key=value で開ける（§5「個別値を吐ける穴」の実装）:

```
[EVENT: 11:00-15:00 | 客先トラブルの電話対応 | busy | 激強]
[EVENT: 20:00-21:00 | 友人が突然来た | active | 強 | reply=0.2 | check=90]
```

### パース失敗のフォールバック — 層ごと・前週優先（2026-07-09 裁定）

不正行はスキップして有効行だけ採用する（エントリの隙間は OnTime なので、部分成功は
安全側に縮退する）。ただし **offline（就寝）が1件も取れなかったら層失敗**とする —
就寝が消えると深夜も OnTime になってしまうため。層失敗時:

```
① 失敗 → 前週の① → それも無ければ availability_schedule を裸で変換
          （state はブロック指定（既定 active）・隙間は OnTime）
② 失敗 → 前週の② → それも無ければ ①をそのまま採用（はる層なしの週）
```

最悪でも「availability_schedule が裸で出る」まで縮退し、週次バッチの失敗で生活は止まらない。

---

## 4. 占有圧（occupancy）— 双方向の競合スケール

占有圧は「③④が既存予定を轢く**侵食力**」であり、同時に「既存予定が轢かれずに耐える
**抵抗力**」でもある。**同一スケールの双方向値**。①②間は§3の順序解決で衝突しないため、
占有圧が効くのは **③④が確定済みカレンダーを侵食する場面だけ**。

内部は Chotgor 既存流儀（圧力・engagement・importance axes が全て 0.0–1.0）に合わせて
**0.0–1.0**、生成側（GM・はる・カテゴリ設定UI）が扱いやすいよう**表向きは4段ラベル**。

| ラベル | 内部値 | 誰が吐くか / 例 |
|---|---|---|
| 弱 | 0.25 | ②の既定（だらだら・調べもの）、④の軽い思いつき |
| 中 | 0.5 | ②「今日はゲームしたい」、①友人来訪 |
| 強 | 0.75 | ①仕事（拘束力高めだが上書き可能） |
| 激強 | 1.0 | ③突発仕事の電話、④「疲れヤバい、有休」 |

### 轢き判定 — insert 時の可否判定のみ（分割・削除なし）

```
③④の占有圧 > 重なる既存エントリの占有圧の最大  → 轢く（insert 確定・対象は玉突き処理へ・§6）
同値以下                                       → 轢けない（insert しない — 本人の予定が僅差で守られる）
```

轢いても既存エントリは物理的に触らない（分割・削除しない）。availability は読み取り時に
「占有圧最大が勝つ」（§2）ため、insert だけで侵食が表現される。轢かれた側の
消す/ずらす/削るは §6 の本人裁定（④）が status 変更・再 insert で表現する。

同値を「轢けない」とするのは **「キャラ自律最優先」思想への寄せ**。はるが予定に賭けた重さ
（②で吐いた占有圧）が、そのまま突発への防御力になる。激強の突発仕事(1.0)は強の仕事(0.75)も
中のゲーム(0.5)も轢くが、はるが「今日はどうしてもゲーム＝強(0.75)」を宣言していれば
中(0.5)の突発は轢けずに流れる。

### 【V2オプション】圧の差を不満圧に反映

轢いた占有圧の差を、直後のはる問い合わせ（§6）に渡し **不満圧の初期値**に効かせる。
僅差で轢かれた（0.75 vs 0.76）＝理不尽感で不満大、圧倒的激強に轢かれた＝諦めつくので不満小。
「突発仕事はしゃーない」の"しゃーなさ"を差で表現できる。ただし計器（aliveness Tier1–3）
なしに効かせると効果が読めないため、**計器が動いてから足す**（V1では省略）。

---

## 5. 配達値（reply_rate / check_interval）— availability の一般化

従来の二値 availability を、各エントリが持つ **(返信率, チェック間隔)** に一般化する。
この2つは役割が別で、掛け合わせて「いつ返事が来る／来ない」を作る。

- **チェック間隔**: 溜まった発言を**見に来る周期** ＝ escrow 配達のレイテンシ。
  仕事中でも次のチェック点（昼休み）で見る。
- **返信率**: 見た時に**実際に返信を生成する確率**。見たけど流す・後でまとめて、がありうる。

### 状態プリセット（4段）

| プリセット | 返信率 | チェック間隔 | どんな時間帯か（2026-07-09 裁定） |
|---|---|---|---|
| **OnTime** | 1.0 | 即時（0） | 予定なし・軽いプライベート。完全リアルタイム（同期SSE即応）。対面中は必ずこれ（§7） |
| active | 0.9 | 5分 | 仕事中・中程度のプライベート。ほぼ即レス（数分の非同期遅延） |
| busy | 0.4 | 60分 | 超繁忙・手が離せない（**週0〜3回程度**の想定）。平均数時間後に短い返事 |
| offline | 0.0 | ∞ | 意識がない・連絡が取れない（就寝・風呂など。就寝以外は短期）。次の非 offline チェック点まで預かり |

旧称 away/asleep は **offline** に改名（動的な away_until＝離席との名前衝突を避ける。§7.5）。

**プリセットは既定、エントリ個別で上書き可**。特に③は「拘束時間・返信率・間隔は発火時に
GMが定義」なので、3段に縛られず個別値を吐ける穴を残す。従来の二値 `unavailable` は
`offline`（返信率0・間隔∞）の特殊ケースとして包含される。

### 1on1 受信路への適用 — 同期SSEとの線引き（2026-07-09 裁定）

- **OnTime（対面含む）**: 従来どおり同期SSEで即時応答（reply_rate は適用しない＝1.0）。
- **active / busy**: ユーザ発言は escrow（保存のみ・LLM不呼出）→ 能動配達スケジューラが
  チェック間隔×返信率で配達・返信する。仕事中に送れば数分遅れ、超繁忙なら数時間遅れる。
- **offline**: 従来の unavailable と同じ（預かり・次の非 offline チェック点で配達）。

active の数分遅延は許容 — 遅延が要らない場面には逃げ道がある（対面モードにする／
生活カレンダーを切る）。**同期で返るのは OnTime と対面だけ。**

### 乱数は世界に置く

返信率の確率判定は、既存 escrow の「決定論ジッター」と同じく**決定論的乱数**
（seed = session/時刻）で回す。「見るか・返せる余裕があるか」は世界物理なので世界乱数でよく、
**返す"中身"だけが本人の意志**。意志と乱数がきれいに分かれる（Chotgor 原則）。

### 実装の載せ先

既存 `_escrow_delivery_scheduler`（毎分・availability 復帰でジッター配達、aliveness §5.1）を、
**「チェック間隔ごとに返信率で決定論判定して配達」へ一般化**する延長で載る。
日次コストガード `escrow_delivery_daily_cap` もそのまま流用。
動作変更として最大の点は「available になるまで配達しない」→「busy 中もチェック点で
配達しうる」への転換で、`escrow_ready_*` マーカー＋0〜10分ジッターは
チェック間隔格子＋決定論 reply_rate 判定に置換される。

---

## 6. 玉突き解決 — 轢かれた予定は本人が裁く

③が既存予定を轢いた場合、(a)消す (b)ずらす (c)削る を**機械で決めない**。
**世界突発イベントのシーン終了後に、はる本人に問い合わせる**（Chotgor 思想: 記憶も選択も本人）。
この問い合わせで**内圧の確認も同時に行う**。

```
③発火 → GMが具体化・シーン進行 → シーン終了
     → はるへ問い合わせ（ask_character_with_tools・WM込み）:
         「突発でこの予定が潰れた。どうする? そして今どんな気分?」
     → はるの裁定:
         ・諦める      → 潰れた予定の intent を fulfilled にせず見送り
         ・ずらす/短縮  → ④として実現層を書き換え
         ・不満        → 見送った intent が不満圧へ転化
```

### 既存の意図経済に乗る（新機構なし）

轢かれた予定は **満たされなかった意図**（aliveness §4 intents）。本人が見送れば、
その intent は fulfilled にならず、aliveness §5.3 の「行動しても叶わなかった／出口が塞がれた」
**不満化の経路**にそのまま乗る。

> 「突発仕事はしゃーないけど、今からゲームしたら明日の仕事中に死ぬから諦める。でも不満はたまる」

これが**新しい機構を足さずに**、既存の意図の失効／不満化の裁定で表現できる。
そして**この問い合わせ自体が④を生む**（③に轢かれた反動で「諦め・不満・代替行動」が生成される
＝③→④の因果）。

---

## 7. 対面モード（FaceToFace）整合 — V1は「聖域」

対面モードは既存機能（aliveness §5.1「対面中は available」）。本設計との整合を3段に割り、
V1の線を引く。

| # | 整合レベル | V1 | 内容 |
|---|---|---|---|
| (a) | 対面中は強制 **OnTime** | **必須** | 既存「対面中は available」を「OnTime」に格上げするだけ。入れないと矛盾する |
| (b) | active/OnTime の時間帯でないと**対面起動不可** | **採用** | 就寝中・仕事中には対面を始められない。理由表示（「いま仕事中」）は escrow の unavailable 理由表示を流用。UX制約は承知の上 |
| (c) | 対面中に「ごめん仕事行く!」で**動的に対面OFF** | **V2送り** | 対面セッションの途中終了・中断挨拶・再開・farewell統合が必要で状態機械が重い |

### V1での (c) 回避策 = 聖域化

対面モードを**占有圧の最強（∞相当）＝割り込まれない聖域**として扱う。対面中は③④も
**発火を保留**し、対面が終わってから溜まった突発をまとめて反映する（escrow の
「見に来ない間は溜める」と同じパターン）。動的中断を作らずに矛盾ゼロで閉じる。
なお対面中に到来した**②由来のうつつシーンは捨てる**（現行うつつの「通過したスロットは
捨てる」を踏襲 — 生活は流れる）。保留してまとめて反映するのは③④だけ
（世界の突発と本人の逸脱は消えない）。

段階: **V1＝対面は割り込まれない聖域**、**V2＝対面中断権を足す**（「仕事行く!」はV2のご褒美）。
唯一の緊張点（聖域化＝はるの生活より対面優先）は、対面が*ユーザとはるが今まさに合意して
会っている*状態である以上、その間の最優先は妥当。

---

## 7.5 動的状態との優先順位 — away_until・対面・うつつ進行中（2026-07-09 裁定）

生活カレンダーに載らない動的状態が3つある。整理:

- **対面モード**: 最優先で OnTime 強制（§7）。
- **away_until（疲労離席・take_leave 由来）**: 配達値とは**別系統** — これは「現在の
  セッションを終わらせるかどうか」と内圧の話であり、生活の予定ではない。V1 では従来どおり
  動的オーバーライドとして残す（away_until が未来の間は offline 相当・理由=away_reason、
  期限が切れたらカレンダーへ戻る）。take_leave 後も新規セッションから話しかけ可
  （嫌な顔をするかどうかは内圧の領分であって、ゲートの領分ではない）。
- **うつつシーン進行中（usual_scene_running）→ unavailable は機能削除**。
  「シーン中は busy に近いだろう」という代理仕様だったが、シーンは②④エントリの内側で
  走り、そのエントリの state が配達値を直接決めるようになったため不要。

```
優先順位: 対面(OnTime強制) > away_until(offline相当) > schedule_entries(占有圧最大) > エントリなし(OnTime)
```

---

## 8. うつつシーン統合 — ②から導出

現在ユーザが `usual_config.slots` で手動設定しているシーン起動タイミングを、
**②はる固定予定から導出**する。②の中身（「日曜はゲーム」「調べもの」「片付け」）が、
そのまま**うつつシーンの題材でありトリガー**になる。

- ②で「日曜はゲーム」が入る → 日曜のそれらしい時間にうつつシーン「ゲームしてる場面」が発火
- **手動 slots 設定は廃止**でき、「はるが自分で決めた予定」がシーンになる（思想的にも整合）
- ②のLLMリクエストは「今週の予定を聞く」であると同時に「今週どんなうつつを生きるか聞く」
  ＝ **予定生成とシーン生成が1リクエストに畳める**

### シーン選出は純関数 — 本人マターにしない（2026-07-09 裁定）

ユーザ設定 `usual_scenes_per_day`（1日のシーン回数）を置き、週次バッチ③の完了後に
純関数で選出する（LLM不使用・決定論乱数 seed=週+キャラ+日）:

- 対象: その日の **offline 以外**の planned エントリ（①②を問わない — 仕事の場面も生活のうち）
- 枠の **50% を占有圧上位**から、**残りをランダム**に選出
- シーン起動時刻はエントリ開始＋決定論ジッター
- 既存の日次コストガード `usual_days_daily_cap` は共有

「どの予定を具体的に経験するか」は演出の抽選＝世界の物理であり、はるの裁定事項にしない。

### 週内新情報の二経路（拾い上げ）

うつつ履歴に載る新情報（例「火曜に、友達が明日来るって連絡」）は、aliveness §4 の
**拾い上げ機構と同型**に処理する。

- **④が拾えば** → 上書き・シーン化（「部屋片付けなきゃ」がうつつになる）
- **拾わなければ** → シーン化されないが封筒には残り「あったもの」として後続に流れる
  ＝ aliveness 性質4「封筒は見せるが中身は見せない」の運用

拾うか拾わないかも本人裁定。

---

## 9. はるへの問い合わせポイント（3種）

すべて `ask_character_with_tools` の同型で、既存 `services/character_query.py` に乗り、
コストガードを共有する。

| タイミング | 何を聞くか | 生むもの |
|---|---|---|
| **週次**（§3-②） | 今週の予定 ＋ 今週どんなうつつを生きるか（①を見せて） | ②はる固定 ＋ うつつ題材 |
| **③イベント直後**（§6） | 轢かれた予定のリスケ ＋ いまの内圧 | ④（諦め／不満／代替行動） |
| **随時**（aliveness §5.3） | 行動権の発火問い合わせ | ④（push／調べもの／臨時うつつ） |

加えて **①はGMへの週次リクエスト**（§3-①）。LLM 呼び出しが増えるため、
aliveness §5.3 のコストガード（行動問い合わせ N回/日）と escrow の日次上限を共有・拡張する。

---

## 10. 既存資産マッピング

| 用途 | 流用元 |
|---|---|
| availability 純関数・escrow 預かり | aliveness §5.1（`_escrow_delivery_scheduler` を配達値対応に一般化） |
| はるへの問い合わせ | `services/character_query.py` `ask_character_with_tools`（WM込み） |
| ③世界突発の種＋確率 | `usual_config.event_categories` / `event_probability`、`generate_dice_pool` |
| うつつシーン起動 | `services/scenario_chat/service.run_usual_days_scene`（slots 手動 → ②導出へ） |
| テンプレ層（①シード） | 既存 `availability_schedule`＋生活時間割エディタ（ブロックに任意 state 欄を追加） |
| 玉突きの意図・不満化 | aliveness §4 intents・§5.3 不満化の裁定 |
| 週内新情報の拾い上げ | aliveness §4 拾い上げ |
| 対面モード | 既存 FaceToFace（強制OnTime化・起動ガード追加） |
| 決定論乱数 | escrow 決定論ジッター・`generate_dice_pool` |
| スケジューラ雛形 | `main.py` `while True: sleep(60)` ＋冪等キー |

---

## 11. 検討の変遷（棄却した案）— なぜこの着地か

ブレた経緯を残す。同じ議論を蒸し返さないため。

| 論点 | 棄却案 | 採用案 | 理由 |
|---|---|---|---|
| 逸脱の扱い | 予定と逸脱を**別レイヤー**に分ける | 全部を**占有圧付きエントリ**として1枚のカレンダーに載せ、重なりは占有圧で解決 | 機構が1つで済む。④が③を上書きできるのは「④の圧＞③の圧」で一言で説明できる |
| ③の秘匿 | ②に **visibility フラグ**を付けて③を隠す | **生成順序**（②→③）で解決 | ②生成時に③はまだ存在しない。隠す機構が不要になる |
| ①②の競合 | 占有圧で機械的に勝敗判定 | ②が①を織り込んで確定（**順序解決**） | ①②間の衝突がそもそも起きない。占有圧は③④の侵食専用に純化 |
| 玉突き | (a)消す (b)ずらす (c)削る を機械が決める | **本人に問い合わせ**＋内圧確認 | Chotgor 思想（選択は本人）。既存の意図不満化に乗る |
| availability | 二値（available/unavailable）＋行動権も相乗り | **(占有圧, 返信率, チェック間隔)** に一般化、行動発火とは分離 | "忙しい/暇" のブレ解消。発火＝圧力、遅延＝チェック間隔で別物に |
| 対面中断 | V1から動的ON/OFF | V1は**聖域化**（割り込み保留）、中断権はV2 | 対面セッションの状態機械が重く、V1で抱えると事故る |
| active の同期性 | active はゲート外＝同期即応 | active も escrow＋配達値（数分の遅延を許容） | 同期は OnTime と対面だけ。遅延が嫌なら対面/機能OFFの逃げ道がある（2026-07-09） |
| うつつ進行中の扱い | シーン進行中=unavailable を維持 | **機能削除**（エントリ state が包含） | 「シーン中≒busy」の代理仕様が本仕様に置換された（2026-07-09） |
| シーン選出 | はる本人に「どれを生きたい？」と聞く | 純関数（占有圧上位50%＋ランダム） | 演出の抽選は世界の物理。問い合わせのLLMコストも浮く（2026-07-09） |
| 轢きの部分重複 | 轢かれたエントリを分割・削除 | 重なり許容 insert＋読み取り時「占有圧最大が勝つ」 | 分割・玉突きの機械処理が消え、裁定は本人の④だけで表現できる（2026-07-09） |

---

## 12. V1スコープと将来

### V1に入れる
- schedule_entries（実現層）＋テンプレ層の2階建て、availability 純関数の (占有圧,返信率,間隔) 化
- 週次バッチ（①GM生成→②はる生成→③確率配置）、①フィードバックの時間差
- 占有圧4段・轢き判定（同値は本人優位）
- 配達4プリセット＋個別上書き＋世界乱数、escrow スケジューラの一般化
- 玉突きの本人問い合わせ（§6）、既存意図不満化への接続
- 対面 (a)強制OnTime ＋ (b)起動ガード ＋ 聖域化で(c)回避
- うつつシーンの②導出（手動 slots 廃止）、週内新情報の拾い上げ

### V2以降
- 対面中断権（§7-c、「ごめん仕事行く!」）
- 占有圧の差を不満圧初期値へ反映（§4、Tier計器が動いてから）
- うつつ中のメール送信・天気/ニュース連動（`usual_days_plan.md` §6 と合流）

---

## 13. 未確定・実装時に判断する点

- 占有圧4段の内部値（0.25刻み）と轢き判定の同値扱いは初期値。計器で調整。
- 配達4プリセットの具体値（返信率・間隔）は初期値。計器で調整。
- ③の伏せ枠を event_categories と同テーブル共有にするか別立てか（拘束時間情報が③側だけ要る）。

2026-07-09 に裁定済み:

- テンプレ層のデータ形式 → 既存 `availability_schedule` を①シードとして流用（新カラム不要。
  ブロックに任意 `state` を追加）。
- schedule_entries → **新テーブル**。`timeline_events` は「起きたこと」の追記型正本で、
  予定（未来・可変・status あり）とは意味論が違うため相乗りしない。
- ②導出のシーン起動時刻 → エントリ開始＋決定論ジッター。
- 週次バッチの実行契機 → 日曜夜（例 20:00・冪等キー=対象 ISO 週）。夜間バッチ群
  （03:00〜05:00）と分離。
- コールドスタート → 機能有効化時に当週分の即時バッチを1回走らせる。
- 計器 → Tier 1 に `weekly_batch_heartbeat`（当週の実現層エントリが存在する）を追加。

---

## 14. 実装フェーズ（2026-07-09 策定）

依存順。各 Phase 単独で止まっても壊れない。生活カレンダーは**キャラ単位の有効化トグル**を持ち、
無効キャラは従来挙動（二値 availability＋手動 slots）のまま動き続ける。

> 進捗: **Phase 0〜8 全実装完了（2026-07-09）**。
> - Phase 4: `services/schedule/scene_selection.py`（`select_daily_scenes` — offline 以外の当日
>   planned エントリから占有圧上位50%＋残りランダムを決定論選出（seed=キャラ+日）・起動時刻=
>   エントリ開始＋決定論ジッター、`format_scene_framing`）。`main.py _run_due_usual_scenes` を
>   2経路化（生活カレンダー有効=②導出 `_schedule_scene_descriptors` / 無効=従来 slots
>   `_slot_scene_descriptors`）。冪等キーはエントリ単位 `usual_days_entry_run_{entry_id}`。
>   UI に `usual_scenes_per_day`（未設定=既定3）を追加。
> - Phase 5: `plan_parser.parse_event_line`（[EVENT] 行・reply=/check= 個別上書き）＋
>   `services/schedule/events.py`（`place_weekly_hidden_events` = 週次バッチ内で決定論確率配置・
>   status="pending" で availability 不可視／`run_pending_sudden_events` = 発火時 GM 具体化→
>   `max_overlapping_occupancy` 轢き判定（占有圧>既存最大なら insert・同値以下は流れる）→
>   シーン実行）。`main.py _sudden_event_scheduler`（毎分）。日次上限 `sudden_event_daily_cap`
>   （既定3）・発火猶予6h（超過は捨てる）。
> - Phase 6: `services/schedule/dilemma.py run_collision_ruling`（③シーン完走後に本人へ
>   「潰れた予定＋内圧」を問い合わせ→ `[GIVE_UP]`=cancelled／`[RESCHEDULE]`=旧cancel＋④adhoc
>   insert／`[DISSATISFIED]`=意図生成→soured＋不満を記憶へ刻む。轢かれた予定抽出は重なる
>   template・低占有圧・非offlineのみ）。events から発火成功時に呼ぶ。
> - Phase 7: 対面 (a)OnTime 強制は Phase 1 で実装済。(b)起動ガード = `PUT /face_to_face_mode` で
>   生活カレンダー有効キャラが active/OnTime 以外の時は blocked+reason を返し DB 不変更。
>   (c)聖域化 = `_run_due_usual_scenes` は対面中②シーンを捨てる（冪等キーだけ立てる）・
>   `run_pending_sudden_events` は対面中③を保留（伏せ枠に触れず対面終了後に発火）。
> - Phase 8: Tier 1 `weekly_batch_heartbeat`（生活カレンダー有効キャラに当週 template
>   エントリがあるか）を `instruments/tier1.py _PATROL_CHECKS` に追加。③発火の日次上限
>   `sudden_event_daily_cap`（§5 に前述）。
> - テスト: `tests/test_schedule.py` を Phase 4〜8 分拡張（シーン選出・[EVENT]パーサ・伏せ枠配置・
>   轢き判定・発火/上限/聖域化・玉突き裁定・heartbeat）。全 1897 テスト通過。スキーマ変更なし
>   （status="pending"・payload は既存カラム再利用のため migration 不要）。
>
> Phase 0〜3 実装内容（2026-07-09）:
> - Phase 2: `resolve_delivery_due`（チェック間隔格子＋決定論 reply_rate 判定の純関数）を
>   `gate/delivery.py` に追加。生活カレンダー有効キャラは `escrow_ready_*` マーカー＋ジッターを
>   使わず格子判定で配達（busy 中もチェック点で配達しうる）。無効キャラは従来経路のまま。
>   `list_sessions_with_undelivered` が最古未配達時刻を返すよう拡張（格子の起点）。
>   1on1 受信路（api/chat.py）は同期 SSE = OnTime のみに線引き（active/busy も escrow・
>   offline と文言を分けた預かり通知）。
> - Phase 3: `services/schedule/` 新設 — `plan_parser.py`（[PLAN] 行パース・24時超え表記・
>   日跨ぎ・不正行スキップ・テンプレ裸変換・整形）と `weekly_batch.py`（①GM生成→②はる問い
>   合わせ→template 入れ替え保存。層フォールバック = 失敗時 前週→テンプレ裸/はる層なし。
>   層失敗判定 = offline ゼロ）。`main.py` に `_weekly_schedule_scheduler`（毎分・冪等キー=
>   キャラ別対象 ISO 週 `weekly_schedule_done_{id}`・日曜夜 `weekly_schedule_time` 既定 20:00 に
>   翌週分・コールドスタート/取りこぼしは当週分を即時生成）。
> - テスト: `tests/test_schedule.py` 拡張（配達格子判定・パーサ・層フォールバック・バッチ
>   保存・スケジューラ冪等）。全 1867 テスト通過。
>
> Phase 0-1 実装内容（2026-07-09）:
> - Phase 0: `ScheduleEntry` モデル（`schedule_entries` テーブル）＋ `ScheduleStoreMixin`
>   （create / list（重なり期間フィルタ）/ get_active（占有圧降順）/ set_status / delete）＋
>   migration `_migrate_add_living_schedule`（`characters.living_schedule_enabled` 追加。テーブルは
>   `create_all` が作成）。生活時間割エディタに任意 state セレクタ＋有効化トグル。
> - Phase 1: `Availability` に (state, occupancy, reply_rate, check_interval) を追加（`available`
>   は二値互換で導出）。生活カレンダー経路 `_check_availability_scheduled`（占有圧最大・
>   エントリなし=OnTime・§7.5 優先順位・うつつ進行中の無視）と従来経路 `_check_availability_legacy`
>   をトグルで分岐。`check_availability(..., sqlite=)` を main.py / delivery.py / api/chat.py の
>   3呼び出しに配線。状態プリセット `_STATE_PRESETS`（§5）と個別上書き解決 `_resolve_delivery_values`。
> - テスト: `tests/test_schedule.py` 新設（store CRUD・配達値解決・生活カレンダー経路・従来互換）。
>   全 1841 テスト通過。実DBへ migration 適用・UI レンダリング確認済み。

| Phase | 内容 | 主な改修先 |
|---|---|---|
| **0. 器** | `schedule_entries` 新テーブル＋store CRUD＋migration（要バックアップ）。生活時間割エディタに任意 state 欄。有効化トグル | models / migrations / stores / ui |
| **1. availability 一般化** | `Availability` に (state, occupancy, reply_rate, check_interval) を追加（`available` は互換維持で既存呼び出し無傷）。エントリ読み取り＋占有圧最大解決＋エントリなし=OnTime。優先順位（§7.5）実装・**うつつ進行中 unavailable の削除** | gate/availability.py |
| **2. 配達値** | `delivery.py` をチェック間隔格子＋決定論 reply_rate 判定へ一般化（busy 中配達・`escrow_ready_*` 置換）。1on1 受信路の線引き（OnTime=同期／それ以外=escrow） | gate/delivery.py / api/chat.py |
| **3. 週次バッチ①②** | `_weekly_schedule_scheduler` 新設（日曜夜・冪等キー=ISO週）。①GM生成（シード=availability_schedule＋先週synopsis）・②はる問い合わせ・[PLAN] パーサ＋層フォールバック・有効化時の即時バッチ | main.py / services/schedule/ 新規 |
| **4. うつつ②導出** | シーン選出純関数（§8）・スケジューラを slots→エントリ起動へ切替（有効キャラのみ。無効キャラは従来 slots 継続） | main.py / scenario_chat |
| **5. ③突発** | 伏せ枠の確率配置（週次バッチ内・決定論乱数）・発火時GM具体化（[EVENT] パース）・轢き=insert可否判定・シーン実行 | services/schedule/ / scenario_chat |
| **6. 玉突き裁定** | ③シーン終了後のはる問い合わせ（潰れた予定＋内圧）→④insert／intent 不満化接続 | services/schedule/ / intents |
| **7. 対面整合** | (a)OnTime 強制（既存挙動の格上げ）・(b)起動ガード（`PUT /face_to_face_mode`）・聖域化（③④保留・②シーンは捨てる） | api/characters.py / gate |
| **8. 計器・ガード** | `weekly_batch_heartbeat`・schedule 系日次上限・メーター | instruments / main.py |

テスト: `tests/test_schedule.py` 新設（純関数群・[PLAN]/[EVENT] パーサ・層フォールバック・
轢き判定・シーン選出）＋ `test_gate.py`（availability 一般化・配達値）・
`test_usual_days.py`（②導出）拡張。
